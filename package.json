{
  "name": "ai-daily-digest-v2",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.11",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check:fork": "bun scripts/check-fork-contract.ts",
    "check:gate": "bun scripts/check-linear-workflow-runtime.ts",
    "check:secrets": "bun scripts/check-candidate-secrets.ts",
    "render:batch": "bun scripts/render-linear-workflow-batch.ts",
    "audit:sources": "bun scripts/audit-sources.ts",
    "check": "bun run typecheck && bun test && bun run check:fork"
  },
  "devDependencies": {
    "@types/bun": "1.3.11",
    "typescript": "5.9.3"
  }
}
